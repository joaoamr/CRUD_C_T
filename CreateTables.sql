CREATE TABLE Cliente (CPF varchar(15) NOT NULL PRIMARY KEY, NOME varchar(300) NOT NULL, DATANASCIMENTO datetime NOT NULL, GENERO char(1) NOT NULL)
CREATE TABLE Telefone (ID integer IDENTITY(1,1) PRIMARY KEY,  DDD varchar(5) NOT NULL, NUMERO varchar(10) NOT NULL, CPF varchar(15) FOREIGN KEY REFERENCES Cliente(CPF))